namespace Codecov.Terminal
{
    internal enum TerminalName
    {
        /// <summary>
        /// A generic termainal to run commands. For example, a powershell, cmd, or bash terminal
        /// can run git commands. Thus, it does not matter what terminal we are using.
        /// </summary>
        Generic
    }
}
